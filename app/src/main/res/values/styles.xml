<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/fontsColorLight</item>
        <item name="android:textColorHint">@color/fontsColorDark</item>
        <item name="colorControlNormal">@color/iconesClick</item>
        <item name="colorControlActivated">@color/titles</item>
    </style>

    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/titles</item>
        <item name="colorControlActivated">@color/iconesClick</item>
    </style>

    <!--Floating label text style-->
    <style name="MyHintText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/fontsColorDark</item>
    </style>

</resources>

